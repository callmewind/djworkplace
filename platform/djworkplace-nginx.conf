server {
    listen 80;

    root /usr/share/nginx/html;

    location /static {
    	alias /static;
    }

    location / {
        include /code/platform/uwsgi_params;
        uwsgi_pass app:3031;
    }
}